[project]
name = "passenger_traffic_analysis"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "colorama>=0.4.6",
    "pydantic>=2.12.5",
    "pywebview>=6.1",
    "sqlalchemy>=2.0.46",
    "structlog>=25.5.0",
]
